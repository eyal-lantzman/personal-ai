from agent.graph import graph
import logging

logger = logging.getLogger(__name__)

# def test_graph():
#     for node in graph.nodes:
#         logger.debug(node)
#     logger.debug(graph.input_channels)
#     assert graph is None